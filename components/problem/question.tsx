import { Card } from "@/components/ui/card";
const Question = () => {
  return (
    <Card className="p-5">
      Qn: What are the main benefits of using TypeScript over JavaScript in a
      large-scale application, and how does TypeScript enhance code
      maintainability and readability?
    </Card>
  );
};

export default Question;
